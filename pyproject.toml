[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = []

[tool.ruff.lint.per-file-ignores]
"scripts/gh_setup_repo_policies.sh" = ["D"]

[tool.ruff.lint.isort]
known-first-party = ["ops", "scripts"]
